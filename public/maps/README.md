# Map images

Drop the B1 map scans here as:

    upper-level.png
    lower-level.png

The dev map panel on the test page loads them from `/maps/`.
Pin positions and the level list live in `src/map/roomCoords.js`.

Note: the module maps are third-party artwork (TSR / Wizards of the Coast).
Fine for a local development tool; revisit before anything is published.
